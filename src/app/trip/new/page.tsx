// ============================================================
// PAGE: New Trip
// OWNER: Track A (Frontend & UX)
// ROUTE: /trip/new
// DESCRIPTION: Alias for the home page's trip initiation flow.
//   Renders TripInitForm and checks passport expiry before the
//   user submits. The mascot greets and listens for the
//   destination via the form.
//
// FRAME: 1 — Identity and Intent Initialization
// ============================================================

// TODO: import { TripInitForm } from "@/components/trip/TripInitForm"
// TODO: import { getServerSession } from "next-auth"
// TODO: import { redirect } from "next/navigation"

// TODO: export default async function NewTripPage() {
//   // const session = await getServerSession()
//   // if (!session) redirect("/login")
//   // return <TripInitForm />
// }

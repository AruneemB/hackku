**Actionable comments posted: 8**  > [!NOTE] > Due to the large number of review comments, Critical severity comments were prioritized as inline comments.  > [!CAUTION] > Some comments are outside the diff and can’t be posted inline due to platform limitations. >  >  >  > <details> > <summary>⚠️ Outside diff range comments (3)</summary><blockquote> >  > <details> > <summary>src/app/api/stt/route.ts (1)</summary><blockquote> >  > `8-17`: _⚠️ Potential issue_ | _🟠 Major_ >  > **Bound and validate audio input before calling Gemini.** >  > This route forwards unbounded base64 to an external model. Add type, MIME, and size checks so malformed or oversized requests fail before consuming memory/API quota. >  > <details> > <summary>Suggested guard</summary> >  > ```diff >  export async function POST(req: NextRequest) { >    const { audio, mimeType } = (await req.json()) as { audio: string; mimeType: string }; >   > -  if (!audio) { > +  if (typeof audio !== "string" || !audio) { >      return NextResponse.json({ error: "No audio data" }, { status: 400 }); >    } >   >    const audioMime = (mimeType || "audio/webm").split(";")[0]; > +  const allowedMimeTypes = new Set(["audio/webm", "audio/wav", "audio/mpeg", "audio/mp4", "audio/ogg"]); > +  if (!allowedMimeTypes.has(audioMime)) { > +    return NextResponse.json({ error: "Unsupported audio type" }, { status: 400 }); > +  } > + > +  const approxBytes = Math.ceil((audio.length * 3) / 4); > +  if (approxBytes > 10 * 1024 * 1024) { > +    return NextResponse.json({ error: "Audio payload too large" }, { status: 413 }); > +  } >   >    try { > ``` >  > </details> >  > <details> > <summary>🤖 Prompt for AI Agents</summary> >  > ``` > Verify each finding against the current code and only fix it if needed. >  > In `@src/app/api/stt/route.ts` around lines 8 - 17, Validate and bound the > incoming audio before calling model.generateContent: ensure the request body > fields audio and mimeType are present and of expected types, check mimeType > (audioMime) against an allowlist (e.g., audio/webm, audio/wav, audio/mpeg), > verify the audio string is valid base64 and decode it to compute the byte > length, enforce a maximum decoded size (e.g., few MBs) and return > NextResponse.json({ error: ... }, { status: 400 }) for > invalid/malformed/oversized payloads; perform these checks right after > extracting { audio, mimeType } and before using audioMime or invoking > model.generateContent to avoid forwarding unbounded data. > ``` >  > </details> >  > </blockquote></details> > <details> > <summary>src/hooks/useMascot.ts (1)</summary><blockquote> >  > `247-270`: _⚠️ Potential issue_ | _🟡 Minor_ >  > **Clear thinking state when speech starts.** >  > Since the mascot renders the thinking image whenever `isThinking` is true, any caller that forgets to clear it before `say(...)` will show the thinking pose while Kelli is speaking. >  >  >  > <details> > <summary>Proposed fix</summary> >  > ```diff >  async function say(text: string, newTone?: ToneKey) { >    if (typeof window === "undefined") return; >  >    clearSpeechWork(); >  >    const tone = newTone ?? mascotState.tone; > -  setMascotState({ speech: text, visibleLength: 0, tone, isSpeaking: true }); > +  setMascotState({ speech: text, visibleLength: 0, tone, isSpeaking: true, isThinking: false }); >  >    const ok = await speakWithElevenLabs(text, tone); >    if (!ok) await speakWithBrowser(text, tone); >  } > @@ >  function stopSpeaking() { >    if (typeof window === "undefined") return; >    clearSpeechWork(); > -  setMascotState({ isSpeaking: false, visibleLength: mascotState.speech.length }); > +  setMascotState({ isSpeaking: false, isThinking: false, visibleLength: mascotState.speech.length }); >  } > ``` > </details> >  > <details> > <summary>🤖 Prompt for AI Agents</summary> >  > ``` > Verify each finding against the current code and only fix it if needed. >  > In `@src/hooks/useMascot.ts` around lines 247 - 270, The mascot can remain in the > "thinking" pose if callers forget to clear isThinking before speaking; update > the say function to explicitly clear thinking when speech starts by setting > isThinking: false as part of the setMascotState call (i.e., in say(), call > setMascotState({ speech: text, visibleLength: 0, tone, isSpeaking: true, > isThinking: false })). This ensures the thinking image is cleared whenever > speakWithElevenLabs/speakWithBrowser runs; leave setThinking and stopSpeaking > behavior unchanged. > ``` >  > </details> >  > </blockquote></details> > <details> > <summary>src/app/demo/page.tsx (1)</summary><blockquote> >  > `1581-1614`: _⚠️ Potential issue_ | _🟠 Major_ >  > **Check the conversation response before using it.** >  > A non-2xx JSON response can flow through as `data`, leaving `data.mascotMessage` undefined and passing that into conversation state and `say(...)`. >  >  >  > <details> > <summary>Proposed fix</summary> >  > ```diff >        const res = await fetch("/api/demo/conversation", { >          method: "POST", >          headers: { "Content-Type": "application/json" }, >          body: JSON.stringify({ messages: msgs, frameIndex: currentIndex, knownFields }), >        }); > +      if (!res.ok) throw new Error("Conversation request failed"); > + >        const data = (await res.json()) as { >          mascotMessage: string; >          tone: Tone; >          extractedData: (Record<string, string> & { return: string }) | null; >          isComplete: boolean; >          knownFields?: Record<string, string>; >        }; > +      if (!data.mascotMessage || !data.tone) { > +        throw new Error("Conversation response missing required fields"); > +      } >  >        if (data.knownFields) setKnownFields(data.knownFields); > ``` > </details> >  > <details> > <summary>🤖 Prompt for AI Agents</summary> >  > ``` > Verify each finding against the current code and only fix it if needed. >  > In `@src/app/demo/page.tsx` around lines 1581 - 1614, The fetch response from > "/api/demo/conversation" is not being validated, so a non-2xx JSON payload can > leave data.mascotMessage undefined and then get pushed into state and passed to > say(); update the handler around the fetch in page.tsx (the try block where res > and data are defined) to first check res.ok and that data.mascotMessage exists > (or throw/return on error), log or handle the error case, and only call > setConversationMessages, setThinking(false), say(...), and setOverlayReady when > the response is valid; refer to the variables res, data, > setConversationMessages, say, setThinking, and setOverlayReady to locate where > to add the validation and error-path handling. > ``` >  > </details> >  > </blockquote></details> >  > </blockquote></details>  <details> <summary>🟠 Major comments (21)</summary><blockquote>  <details> <summary>src/lib/utils/currency.ts-23-37 (1)</summary><blockquote>  `23-37`: _⚠️ Potential issue_ | _🟠 Major_  **`convertCurrency` lacks timeout, caching, and rate-limit handling.**  Three concerns on this hot path (invoked on every receipt scan/save and per-receipt in the trip receipts route):  1. **No timeout** — an `open.er-api.com` hang stalls the caller request thread. 2. **No caching** — exchange rates change at most daily, but every receipt refetches rates. Under any meaningful volume this will hit the free tier's rate limits and dominate P95 latency. 3. **Throw on any network blip** — the trip receipts route (`src/app/api/trips/[id]/receipts/route.ts:48-72`) calls this without try/catch, so a transient upstream outage fails the user's receipt upload. The scan route wraps it in try/catch but the two paths have inconsistent semantics for the same function.  Recommend an in-memory daily cache keyed by `fromCurrency` plus `AbortSignal.timeout(...)`, and decide one contract (throw vs. return input) and apply it consistently at call sites.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/lib/utils/currency.ts` around lines 23 - 37, convertCurrency currently blocks on network, refetches rates per-call, and throws on transient failures; implement an in-memory TTL cache (Map keyed by fromCurrency.toUpperCase()) that stores rate objects with a 24h expiry, fetch rates only on cache miss, and use AbortController.timeout (or AbortSignal.timeout) with a short timeout (e.g. 2s) when calling fetch to avoid hangs; on fetch/network errors return the original amount (do not throw) so callers like the trip receipts route and the scan route get consistent non-throwing semantics (update callers if they rely on try/catch), and ensure lookup uses toCurrency.toUpperCase() when reading cached rates and when caching new data. ```  </details>  </blockquote></details> <details> <summary>src/lib/utils/pii.ts-15-24 (1)</summary><blockquote>  `15-24`: _⚠️ Potential issue_ | _🟠 Major_  **PII regex coverage has notable gaps.**  A few concerns worth tightening before relying on this for compliance:  1. `CARD_NUMBER_REGEX = /\b(?:\d[ -]*?){13,16}\b/g` — the lazy quantifier inside the repeated group plus `\b` boundaries makes this fragile. It can under-match grouped PANs (e.g. `4111-1111-1111-1111` where separators consume into the group count) and over-match long digit runs like phone numbers or order IDs. Consider an explicit alternation for the standard 13/15/16-digit PAN formats and follow with a Luhn check to avoid false positives. 2. `CVV_REGEX` only fires when the literal `cvv` label is present. Gemini extractions often include a bare 3–4 digit `sec: 123` / `code: 1234` next to the card line; these will slip through. 3. `SSN_REGEX` only matches the hyphenated `XXX-XX-XXXX` form. Unformatted 9-digit SSNs (common in scanned docs) are not stripped. 4. The file header advertises stripping "full names that appear on receipts" but no name masking is implemented. Either remove the promise from the comment or add a name heuristic.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/lib/utils/pii.ts` around lines 15 - 24, Update maskString and the regex constants to improve PII coverage: replace CARD_NUMBER_REGEX with explicit alternation patterns for common PAN groupings (e.g., 13/15/16 digit forms like \d{4}(-|\s)?\d{4}(-|\s)?\d{4}(-|\s)?\d{1,4}) and add a Luhn checksum filter in the masking flow to avoid false positives; broaden CVV_REGEX to also catch unlabeled 3–4 digit security codes adjacent to card-related words (e.g., detect patterns like /\b(?:cvv|cvc|sec|code)\b[:\s-]*\d{3,4}/i and a fallback for isolated 3–4 digit tokens near PAN matches in maskString); extend SSN_REGEX to also match unformatted 9-digit sequences (e.g., /\b\d{3}-\d{2}-\d{4}\b|\b\d{9}\b/g) and validate contextual heuristics to reduce false hits; and either implement a simple name-masking heuristic in maskString (e.g., mask capitalized two-word sequences near receipt keywords) or remove the “full names” promise from the header comment so the file doesn’t claim name stripping. Ensure all replacements still call maskString and preserve existing replacement ordering. ```  </details>  </blockquote></details> <details> <summary>src/app/api/trips/[id]/receipts/route.ts-39-63 (1)</summary><blockquote>  `39-63`: _⚠️ Potential issue_ | _🟠 Major_  **Normalize input and extracted receipt values before storing.**  Unlike `/api/receipt/scan`, this route does not strip a `data:*;base64,` prefix. It also accepts partial/invalid amounts via `parseFloat` and discards the extracted transaction date.  <details> <summary>Suggested normalization</summary>  ```diff -    const raw = await extractReceiptData(imageBase64, mimeType ?? "image/jpeg") +    const base64Data = +      typeof imageBase64 === "string" +        ? imageBase64.replace(/^data:[^;]+;base64,/, "") +        : "" +    const raw = await extractReceiptData(base64Data, mimeType ?? "image/jpeg")      const clean = sanitizeReceiptData(raw)   -    const amountNum = parseFloat(clean.amount) +    const amountNum = Number(clean.amount) +    if (!Number.isFinite(amountNum) || amountNum < 0) { +      return NextResponse.json( +        { error: "Invalid amount extracted from receipt" }, +        { status: 422 } +      ) +    } + +    const extractedDate = new Date(clean.date) +    const receiptDate = Number.isNaN(extractedDate.getTime()) ? new Date() : extractedDate      const totalUsd = await convertCurrency(amountNum, clean.currency, "USD") @@ -      date: new Date(), +      date: receiptDate, ```  </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/trips/`[id]/receipts/route.ts around lines 39 - 63, The route must normalize the incoming image (strip a possible "data:*;base64," prefix from imageBase64 before calling extractReceiptData), validate and normalize the parsed amount (use a robust number parse with fallback and reject/400 on NaN instead of silently accepting parseFloat(clean.amount) — reference amountNum and originalDecimal/toDecimal128), and preserve a valid transaction date from the extracted data (use clean.date or raw.date, parse into a JS Date and fall back to new Date() only if invalid) before building the receipt object; update the flow around extractReceiptData, sanitizeReceiptData, amountNum, totalUsd/totalDecimal, originalDecimal and the receipt.date assignment to perform these steps and return a 400 for invalid inputs. ```  </details>  </blockquote></details> <details> <summary>src/app/api/receipt/scan/route.ts-36-54 (1)</summary><blockquote>  `36-54`: _⚠️ Potential issue_ | _🟠 Major_  **Validate extracted amounts and preserve the PII detection signal.**  Line 52 always returns `false` after sanitization, even when Gemini detected PII. Also, Lines 39-41 can turn malformed AI output into `"NaN"` in the response.  <details> <summary>Suggested adjustment</summary>  ```diff      const raw = await extractReceiptData(base64Data, mimeType)      const clean = sanitizeReceiptData(raw)   -    let totalUsd = parseFloat(clean.amount) +    const amountNum = Number(clean.amount) +    if (!Number.isFinite(amountNum) || amountNum < 0) { +      return NextResponse.json( +        { error: "Invalid amount extracted from receipt" }, +        { status: 422 } +      ) +    } + +    let totalUsd = amountNum      try { -      totalUsd = await convertCurrency(parseFloat(clean.amount), clean.currency, "USD") +      totalUsd = await convertCurrency(amountNum, clean.currency, "USD")      } catch {        // Conversion failed — fall back to original amount and note it      }   @@        currency: clean.currency,        date: clean.date,        category: clean.category ?? "other", -      hasPII: clean.hasPII, +      hasPII: raw.hasPII, +      sanitized: true,        confidence: raw.confidence,        totalUsd: totalUsd.toFixed(2), ```  </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/receipt/scan/route.ts` around lines 36 - 54, The response currently loses the AI PII detection and may return "NaN" amounts: when building the JSON use a validated numeric totalUsd (parse and check isFinite before using or falling back to a safe default) and preserve the original PII signal from the extractor instead of overwriting it with sanitizeReceiptData; specifically, in the route that calls extractReceiptData, sanitizeReceiptData, convertCurrency and returns NextResponse.json, validate clean.amount with Number.parseFloat and isFinite (use the parsed value only if valid, otherwise fallback to the original string or 0), and return hasPII as (raw.hasPII ?? clean.hasPII) so Gemini-detected PII is not lost. ```  </details>  </blockquote></details> <details> <summary>src/lib/gemini/multimodal.ts-37-40 (1)</summary><blockquote>  `37-40`: _⚠️ Potential issue_ | _🟠 Major_  **Validate Gemini JSON before trusting it.**  Line 40 only casts the parsed object; it does not guarantee `amount`, `currency`, `category`, or `confidence` are usable. Downstream routes parse and persist these fields directly.  <details> <summary>Suggested validation guard</summary>  ```diff      const result = await geminiModel.generateContent([RECEIPT_PROMPT, imagePart])      const text = result.response.text().trim()      const jsonText = text.replace(/^```(?:json)?\s*/i, "").replace(/\s*```$/i, "") -    return JSON.parse(jsonText) as GeminiReceiptExtraction +    const parsed = JSON.parse(jsonText) as Partial<GeminiReceiptExtraction> +    const amount = Number(parsed.amount) +    const categories = new Set(["meal", "transport", "hotel", "other"]) + +    if ( +      typeof parsed.merchant !== "string" || +      !parsed.merchant.trim() || +      !Number.isFinite(amount) || +      amount < 0 || +      typeof parsed.currency !== "string" || +      !/^[A-Z]{3}$/i.test(parsed.currency) || +      typeof parsed.date !== "string" || +      !categories.has(parsed.category ?? "") || +      typeof parsed.hasPII !== "boolean" || +      typeof parsed.confidence !== "number" || +      parsed.confidence < 0 || +      parsed.confidence > 1 +    ) { +      throw new Error("Invalid receipt extraction returned from Gemini") +    } + +    return { +      ...parsed, +      merchant: parsed.merchant.trim(), +      amount: amount.toFixed(2), +      currency: parsed.currency.toUpperCase(), +    } as GeminiReceiptExtraction ```  </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/lib/gemini/multimodal.ts` around lines 37 - 40, The Gemini response JSON is currently parsed and cast directly to GeminiReceiptExtraction without validation; change the code that handles result.response.text() (the generateContent call with RECEIPT_PROMPT and imagePart) to parse into a Partial<GeminiReceiptExtraction>, validate required fields (merchant string non-empty, amount is finite number >=0, currency is a 3-letter code, date is string, category is one of allowed values e.g. "meal","transport","hotel","other", hasPII is boolean, confidence is number between 0 and 1), normalize values (trim merchant, format amount to two decimals, uppercase currency) and throw a descriptive Error if validation fails before returning the object as GeminiReceiptExtraction so downstream code can safely consume fields. ```  </details>  </blockquote></details> <details> <summary>src/app/api/receipt/save/route.ts-22-57 (1)</summary><blockquote>  `22-57`: _⚠️ Potential issue_ | _🟠 Major_  **Require a real trip association and validate receipt fields before inserting.**  Line 38 persists every receipt under the same placeholder `tripId`, and Lines 29-44 allow malformed amounts/dates to reach `toDecimal128()`/MongoDB. That can create orphaned or unusable receipt records.  <details> <summary>Suggested direction</summary>  ```diff -const PLACEHOLDER_TRIP_ID = "PLACEHOLDER_TRIP_ID_000000000000" -  export async function POST(req: NextRequest) {    try { -    const { merchant, category, totalUsd, currency, originalAmount, date } = +    const { tripId, merchant, category, totalUsd, currency, originalAmount, date } =        await req.json()   -    if (!merchant || !totalUsd) { +    const parsedTotalUsd = Number(totalUsd) +    const parsedOriginalAmount = Number(originalAmount ?? totalUsd) +    const receiptDate = date ? new Date(date) : new Date() + +    if ( +      !tripId || +      typeof merchant !== "string" || +      !merchant.trim() || +      !Number.isFinite(parsedTotalUsd) || +      parsedTotalUsd < 0 || +      !Number.isFinite(parsedOriginalAmount) || +      parsedOriginalAmount < 0 || +      Number.isNaN(receiptDate.getTime()) +    ) {        return NextResponse.json( -        { error: "merchant and totalUsd are required" }, +        { error: "tripId, merchant, valid totalUsd, and valid date are required" },          { status: 400 }        )      }        const now = new Date()      const doc = { -      tripId: PLACEHOLDER_TRIP_ID, -      merchant, +      tripId, +      merchant: merchant.trim(),        category: category ?? "other", -      total: toDecimal128(totalUsd), +      total: toDecimal128(parsedTotalUsd),        currency: currency ?? "USD", -      originalAmount: toDecimal128(originalAmount ?? totalUsd), -      date: date ? new Date(date) : now, +      originalAmount: toDecimal128(parsedOriginalAmount), +      date: receiptDate, ```  </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/receipt/save/route.ts` around lines 22 - 57, The POST handler currently saves receipts with a constant PLACEHOLDER_TRIP_ID and does not validate numeric/date fields before calling toDecimal128, so fix POST to require a real tripId (reject and return 400 if missing/invalid) instead of using PLACEHOLDER_TRIP_ID, validate totalUsd and originalAmount are finite numbers (parse/convert and reject on NaN/negative), validate/parse date safely (reject invalid dates or default only if explicitly allowed), and only call toDecimal128 after validation; update any references to PLACEHOLDER_TRIP_ID to stop using it for inserts and ensure the inserted document uses the validated tripId and validated/converted fields or return proper error responses instead of inserting malformed records. ```  </details>  </blockquote></details> <details> <summary>src/app/api/trips/[id]/receipts/route.ts-68-75 (1)</summary><blockquote>  `68-75`: _⚠️ Potential issue_ | _🟠 Major_  **Make the spend-total update atomic.**  Lines 69–74 implement a read-modify-write pattern that is not atomic. Concurrent receipt uploads can both read the same `currentTotal`, compute the same new total, and overwrite each other, causing undercounting. Additionally, converting `Decimal128` to float via `parseFloat()` and performing float arithmetic (`currentTotal + totalUsd`) introduces rounding errors before converting back.  Since `totalSpendUsd` is stored as `Decimal128` in MongoDB, use the atomic `$inc` operator instead:  ```javascript await Trip.findByIdAndUpdate(id, {   $push: { receipts: receipt },   $inc: { totalSpendUsd: totalDecimal }, }) ```  This increments the stored decimal directly without reading the current value, eliminating both the race condition and floating-point arithmetic on the total.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/trips/`[id]/receipts/route.ts around lines 68 - 75, The current read-modify-write using parseFloat and toDecimal128 (variables currentTotal/newTotal) is not atomic and risks lost updates and rounding; modify the Trip.findByIdAndUpdate call to remove the read of trip.totalSpendUsd and instead $inc the Decimal128 amount (use the already-prepared decimal for the receipt total, e.g., totalDecimal/totalUsdDecimal) while still $push-ing the receipt so the update is atomic (replace the $set of totalSpendUsd with $inc on totalSpendUsd and remove parseFloat/currentTotal/newTotal calculations). ```  </details>  </blockquote></details> <details> <summary>src/app/api/auth/gmail-test/route.ts-41-64 (1)</summary><blockquote>  `41-64`: _⚠️ Potential issue_ | _🟠 Major_  **Avoid returning inbox metadata from a test endpoint.**  This exposes Gmail `Subject`, `From`, and `Date` values to the browser just to verify connectivity. Prefer returning a count/status, or gate the detailed metadata behind a development/admin-only path.   <details> <summary>🔒 Proposed safer response</summary>  ```diff -    const messageIds: string[] = (list.messages ?? []).map((m: { id: string }) => m.id) - -    // Fetch subject + from + date for each -    const messages = await Promise.all( -      messageIds.map(async (id) => { -        const msg = await gmailFetch( -          `/messages/${id}?format=metadata&metadataHeaders=Subject&metadataHeaders=From&metadataHeaders=Date`, -          session.accessToken -        ) -        const headers: { name: string; value: string }[] = msg.payload?.headers ?? [] -        const get = (name: string) => -          headers.find((h) => h.name.toLowerCase() === name.toLowerCase())?.value ?? "" -        return { id, subject: get("Subject"), from: get("From"), date: get("Date") } -      }) -    ) - -    return NextResponse.json({ email: session.user?.email, messages }) +    return NextResponse.json({ +      email: session.user?.email, +      gmailConnected: true, +      messageCount: list.resultSizeEstimate ?? 0, +    }) ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/auth/gmail-test/route.ts` around lines 41 - 64, The route currently fetches and returns Gmail header metadata in src/app/api/auth/gmail-test/route.ts (the gmailFetch calls and the NextResponse.json response), which exposes Subject/From/Date to the browser; change the handler so it does not return message headers — instead return only a connectivity/status object or a simple count (e.g., number of messages fetched) and any non-sensitive status flags, or gate the detailed metadata behind a dev/admin-only check using the existing session.user role before including headers; update the code paths that build the messages array (the messageIds map and Promise.all block) to either omit headers entirely or only run when the session is authorized for admin/dev, and ensure NextResponse.json sends the sanitized payload. ```  </details>  </blockquote></details> <details> <summary>src/app/receipt/saved/page.tsx-33-38 (1)</summary><blockquote>  `33-38`: _⚠️ Potential issue_ | _🟠 Major_  **Handle non-OK and non-array receipt responses before rendering.**  Right now a 500 JSON body like `{ error: "..." }` is stored as `receipts`, which will later break at `receipts.map(...)`. Check `response.ok` and validate the payload shape before updating state.   <details> <summary>🧯 Proposed fetch hardening</summary>  ```diff    useEffect(() => { -    fetch("/api/receipt/list") -      .then((r) => r.json()) -      .then((data) => { setReceipts(data); setLoading(false) }) -      .catch((e) => { setError(e.message); setLoading(false) }) +    let cancelled = false + +    async function loadReceipts() { +      try { +        const response = await fetch("/api/receipt/list") +        const data = await response.json() + +        if (!response.ok) { +          throw new Error(data?.error ?? "Failed to load receipts") +        } +        if (!Array.isArray(data)) { +          throw new Error("Invalid receipts response") +        } + +        if (!cancelled) setReceipts(data) +      } catch (e) { +        if (!cancelled) { +          setError(e instanceof Error ? e.message : "Failed to load receipts") +        } +      } finally { +        if (!cancelled) setLoading(false) +      } +    } + +    void loadReceipts() +    return () => { +      cancelled = true +    }    }, []) ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/receipt/saved/page.tsx` around lines 33 - 38, The fetch in useEffect that calls "/api/receipt/list" should check response.ok and validate the JSON shape before calling setReceipts; change the flow inside the useEffect to await or chain the Response, call response.json() only after verifying response.ok (and on non-ok call setError with a message and setLoading(false)), then verify the parsed payload is an array (Array.isArray(payload)) before setReceipts, otherwise setError to a clear message; ensure setLoading(false) runs in all branches and avoid setting receipts to non-array values so later receipts.map(...) cannot crash. ```  </details>  </blockquote></details> <details> <summary>src/types/next-auth.d.ts-18-34 (1)</summary><blockquote>  `18-34`: _⚠️ Potential issue_ | _🟠 Major_  **Make OAuth token fields optional in the augmentation.**  NextAuth v4 does not guarantee `access_token` and `refresh_token` presence on every JWT callback invocation. However, these declarations make tokens and `id` appear required, which masks missing-token paths in TypeScript. Runtime code already checks `if (!session.accessToken)`, confirming nullable behavior should be reflected in types.  <details> <summary>🛠️ Proposed type correction</summary>  ```diff  declare module "next-auth" {    interface Session { -    accessToken: string -    refreshToken: string +    accessToken?: string +    refreshToken?: string      user: DefaultSession["user"] & { -      id: string +      id?: string      }    }  }    declare module "next-auth/jwt" {    interface JWT { -    accessToken: string -    refreshToken: string -    id: string +    accessToken?: string +    refreshToken?: string +    id?: string    }  } ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/types/next-auth.d.ts` around lines 18 - 34, The Session and JWT augmentation currently marks accessToken, refreshToken, and id as required; change them to optional to reflect NextAuth v4 behavior by making accessToken?: string, refreshToken?: string on the declared Session and JWT interfaces and id?: string on the JWT interface so existing runtime checks like if (!session.accessToken) align with types; update the declarations inside the module augmentations for "next-auth" (Session) and "next-auth/jwt" (JWT) accordingly. ```  </details>  </blockquote></details> <details> <summary>src/app/api/hotels/search/route.ts-23-34 (1)</summary><blockquote>  `23-34`: _⚠️ Potential issue_ | _🟠 Major_  **Validate input types and the date range before searching.**  Truthy non-string values can crash at `city.toLowerCase()`, and invalid or reversed dates can produce `NaN`/negative `nights` while still calling `searchHotels`.    <details> <summary>🛡️ Proposed validation tightening</summary>  ```diff -    const { city, country, checkIn, checkOut } = await req.json() +    const body = await req.json().catch(() => null) +    const city = typeof body?.city === "string" ? body.city.trim() : "" +    const country = typeof body?.country === "string" ? body.country.trim() : "" +    const checkIn = typeof body?.checkIn === "string" ? body.checkIn : "" +    const checkOut = typeof body?.checkOut === "string" ? body.checkOut : ""       if (!city || !country || !checkIn || !checkOut) {        return NextResponse.json(          { error: "city, country, checkIn, and checkOut are required" },          { status: 400 }        )      } + +    const checkInMs = Date.parse(`${checkIn}T00:00:00Z`) +    const checkOutMs = Date.parse(`${checkOut}T00:00:00Z`) +    if (!Number.isFinite(checkInMs) || !Number.isFinite(checkOutMs) || checkOutMs <= checkInMs) { +      return NextResponse.json( +        { error: "checkOut must be after a valid checkIn date" }, +        { status: 400 } +      ) +    } +    const nights = Math.round((checkOutMs - checkInMs) / 86_400_000)       const policy = budgetCaps.cities.find(        (c) => c.country === country && c.city.toLowerCase() === city.toLowerCase()      ) ```  ```diff -        nights: Math.round( -          (new Date(checkOut).getTime() - new Date(checkIn).getTime()) / 86_400_000 -        ), +        nights, ``` </details>   Also applies to: 61-63  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/hotels/search/route.ts` around lines 23 - 34, Ensure request fields are type-validated and date-range checked before using them: verify that req.json() yields strings for city and country (or coerce safely) before calling city.toLowerCase(), and parse/check checkIn and checkOut as valid dates with checkOut > checkIn and computed nights > 0; if any validation fails return a 400 error. Update the block around the budgetCaps.cities.find call to use the validated/coerced city/country values, and add the same date validation / nights check before calling searchHotels (referencing searchHotels and the computed nights) to avoid NaN/negative values. ```  </details>  </blockquote></details> <details> <summary>src/app/trip/[id]/planning/page.tsx-108-112 (1)</summary><blockquote>  `108-112`: _⚠️ Potential issue_ | _🟠 Major_  **Only advance after hotel persistence succeeds.**  `updateHotels` currently catches errors internally, so this `await` can resolve even when the PATCH failed. Return a success value or rethrow from `updateHotels` before moving to `"policy"`.    <details> <summary>🛠️ Suggested calling pattern</summary>  ```diff    const handleSelectHotel = async (hotel: Hotel) => { -    await updateHotels([hotel]); +    const saved = await updateHotels([hotel]); +    if (!saved) { +      setHotelError("Failed to save selected hotel. Please try again."); +      return; +    }      say("Perfect! Let me check your travel policy compliance.", "neutral");      setStep("policy");    }; ```  This assumes `updateHotels` is changed to return `true` on success and `false` or throw on failure. </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/trip/`[id]/planning/page.tsx around lines 108 - 112, updateHotels currently swallows errors so awaiting it in handleSelectHotel can falsely succeed; change updateHotels to either rethrow errors or return a boolean success flag (e.g., return true on success, false or throw on failure) and then update handleSelectHotel to check that return value (or wrap the call in try/catch) before calling say("Perfect! ...") and setStep("policy"); if the save fails, do not advance step and instead surface the error (log and/or inform the user) so persistence is guaranteed before moving to the "policy" step. ```  </details>  </blockquote></details> <details> <summary>src/app/auth/page.tsx-211-216 (1)</summary><blockquote>  `211-216`: _⚠️ Potential issue_ | _🟠 Major_  **Don’t render OAuth token fragments in the browser.**  Even tails of access/refresh tokens are sensitive debugging artifacts and can leak via screenshots or support recordings. Show boolean scope/session status instead.    <details> <summary>🛡️ Proposed safer token status UI</summary>  ```diff  function TokenCard({ session }: { session: Session }) {    return (      <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 flex flex-col gap-2 font-mono text-xs"> -      <Row label="accessToken"  value={session.accessToken  ? `…${session.accessToken.slice(-8)}`  : "missing"} ok={!!session.accessToken} /> -      <Row label="refreshToken" value={session.refreshToken ? `…${session.refreshToken.slice(-8)}` : "missing"} ok={!!session.refreshToken} /> +      <Row label="accessToken" value={session.accessToken ? "present" : "missing"} ok={!!session.accessToken} /> +      <Row label="refreshToken" value={session.refreshToken ? "present" : "missing"} ok={!!session.refreshToken} />      </div>    )  } ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/auth/page.tsx` around lines 211 - 216, TokenCard currently exposes token tails by using session.accessToken.slice(...) and session.refreshToken.slice(...); remove any rendering of token fragments and instead display non-sensitive status strings (e.g., "present"/"missing" or "available"/"not available") or rely solely on the existing ok boolean passed to Row. Update TokenCard to stop referencing session.accessToken.slice and session.refreshToken.slice, change the Row value props to a safe status string, and ensure no other code in TokenCard or Row logs or serializes token contents. ```  </details>  </blockquote></details> <details> <summary>src/lib/google/oauth.ts-39-50 (1)</summary><blockquote>  `39-50`: _⚠️ Potential issue_ | _🟠 Major_  **Remove refresh token from client session and implement token refresh logic in JWT callback.**  Lines 48-49 expose `refreshToken` to client-side `useSession()`, and `getAccessToken()` (lines 59-61) returns an access token without checking expiration. Refresh tokens should remain server-side only. Add expiration checks and token refresh rotation in the JWT callback using Google's token endpoint before returning expired access tokens.  Required changes: - Remove `session.refreshToken = token.refreshToken` (line 49) - Update JWT callback to track `expires_at` and call Google's `/oauth2/token` endpoint with `grant_type=refresh_token` when expired - Update `getAccessToken()` to call the refreshed token from JWT, or better, return the current valid token directly    ``` Lines 59-61 ```  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/lib/google/oauth.ts` around lines 39 - 50, Remove exposing refreshToken in the session by deleting the assignment in the session callback (remove session.refreshToken = token.refreshToken) and keep refresh tokens only in the JWT token store; modify the jwt callback (async jwt({ token, account, user })) to persist expires_at (e.g., from account.expires_at or compute Date.now()+expires_in*1000) and implement refresh logic: when an incoming jwt is expired, call Google's token endpoint /oauth2/token with grant_type=refresh_token and the stored refresh token to obtain a new access_token (and rotate refresh_token if returned), update token.accessToken, token.refreshToken (if rotated), and token.expires_at before returning the token; finally update getAccessToken() to read and return the valid token from the JWT (triggering/using the jwt refresh flow) instead of returning token.accessToken blindly so callers always get a non-expired token. ```  </details>  </blockquote></details> <details> <summary>src/app/api/trips/[id]/approve/route.ts-42-70 (1)</summary><blockquote>  `42-70`: _⚠️ Potential issue_ | _🟠 Major_  **Make approval sending idempotent before the Gmail side effect.**  Two concurrent requests can both pass the `gmailThreadId` check and send duplicate emails. Also, if Gmail succeeds but the DB update fails, retrying sends another email because the thread ID was never persisted. Use an atomic reservation/outbox pattern before calling Gmail.    <details> <summary>🛡️ Suggested direction</summary>  ```diff -    // Don't re-send if already pending/approved -    if (trip.approvalThread?.gmailThreadId) { +    // Don't re-send if already sent. +    if (trip.approvalThread?.gmailThreadId) {        return NextResponse.json({          success: true,          gmailThreadId: trip.approvalThread.gmailThreadId,          alreadySent: true,        })      } + +    // Reserve the approval flow atomically before the non-idempotent Gmail send. +    // Consider a dedicated outbox collection/job for full retry safety. +    const reservedTrip = await Trip.findOneAndUpdate( +      { _id: id, userId: session.user.id, "approvalThread.gmailThreadId": null, "approvalThread.status": { $ne: "pending" } }, +      { status: "pending_approval", "approvalThread.status": "pending" }, +      { new: true } +    ) +    if (!reservedTrip) { +      return NextResponse.json({ error: "Approval request already in progress" }, { status: 409 }) +    } ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/trips/`[id]/approve/route.ts around lines 42 - 70, Race condition: two requests can both see no gmailThreadId and both call sendApprovalRequest, and if Gmail succeeds but DB persist fails you'll resend; fix by performing an atomic reservation in the DB before calling Gmail: use a conditional findOneAndUpdate (or findByIdAndUpdate with a filter) on the Trip document that requires approvalThread.gmailThreadId to be null/undefined and status not "pending_approval" and set a temporary reservation value/flag (e.g. approvalThread.gmailThreadId = "__reserving__" and approvalThread.status = "reserving"); only call sendApprovalRequest if that update returns the modified document (i.e. you won the reservation); after sendApprovalRequest, atomically update the Trip with the real threadId and status "pending_approval"; if sendApprovalRequest fails, clear the reservation (unset gmailThreadId/status) so retries can proceed; reference Trip.findByIdAndUpdate / findOneAndUpdate, sendApprovalRequest, trip.approvalThread.gmailThreadId, and the status fields when making these changes. ```  </details>  </blockquote></details> <details> <summary>src/app/api/auth/gmail-send/route.ts-22-38 (1)</summary><blockquote>  `22-38`: _⚠️ Potential issue_ | _🟠 Major_  **Block CRLF header injection in both email-building functions.**  The `buildRfc2822` function at lines 22–39 directly interpolates `to`, `from`, and `subject` into RFC 2822 headers without validation. A value containing `\r` or `\n` can inject additional headers (e.g., `Bcc:`) or alter the message body. This affects both the public API endpoint (`src/app/api/auth/gmail-send/route.ts`) and the shared helper (`src/lib/google/gmail.ts:buildRfc2822` called by `sendApprovalRequest`).  Input validation in the POST handler must reject CR/LF characters before passing values to `buildRfc2822`. Additionally, the shared helper function should validate all header parameters to prevent injection regardless of the caller.  Proposed validation:  <details> <summary>🛡️ Input validation in POST handler</summary>  ```diff    const { to, subject, body } = await req.json()    if (!to || !subject || !body) {      return NextResponse.json({ error: "to, subject, and body are required" }, { status: 400 })    } +  if ([to, subject].some((value) => typeof value !== "string" || /[\r\n]/.test(value))) { +    return NextResponse.json({ error: "Invalid email header value" }, { status: 400 }) +  } ```  </details>  <details> <summary>🛡️ Header validation in buildRfc2822</summary>  ```diff +function assertHeaderValue(name: string, value: string) { +  if (/[\r\n]/.test(value)) { +    throw new Error(`${name} cannot contain line breaks`) +  } +} +  function buildRfc2822(to: string, from: string, subject: string, body: string): string { +  assertHeaderValue("from", from) +  assertHeaderValue("to", to) +  assertHeaderValue("subject", subject) +    const message = [ ```  </details>  Apply the same validation in `src/lib/google/gmail.ts` to protect the `sendApprovalRequest` flow.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/auth/gmail-send/route.ts` around lines 22 - 38, The buildRfc2822 function is vulnerable to CR/LF header injection because it directly interpolates to/from/subject; add strict validation in buildRfc2822 (and mirror it in the shared src/lib/google/gmail.ts version used by sendApprovalRequest) to reject or throw on any inputs that contain '\r' or '\n' (for header fields: to, from, subject) and only allow a safe subset (e.g., printable characters / whitespace but no CR/LF); additionally, in the POST handler in src/app/api/auth/gmail-send/route.ts validate the incoming request body (to, from, subject) and return an error response if CR or LF are present before calling buildRfc2822 so untrusted input is never passed through. ```  </details>  </blockquote></details> <details> <summary>src/app/trip/[id]/approval/page.tsx-148-155 (1)</summary><blockquote>  `148-155`: _⚠️ Potential issue_ | _🟠 Major_  **Rejection path is a no-op.**  `onRejected={() => {}}` means that once `ApprovalStatus` observes `status === "rejected"` it will play the mascot line and then… nothing. No navigation, no UI switch, no `RejectionRecovery` despite the header comment advertising it. At minimum, surface the rejection reason on this page (e.g., toggle a `rejected` state and render the `RejectionRecovery` component, or route to an alternative-options screen).  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/trip/`[id]/approval/page.tsx around lines 148 - 155, The onRejected prop passed to ApprovalStatus is a no-op, so rejected approvals do nothing; update the handler (the onRejected prop in the ApprovalStatus usage) to surface the rejection reason and show recovery UI: either set a local rejected state (e.g., setRejected(true)/setRejectionReason(...)) and conditionally render the RejectionRecovery component on this page, or navigate to an alternative route (e.g., router.push(`/trip/${tripId}/recovery`)) and pass the reason; ensure the handler forwards any rejection payload from ApprovalStatus so the RejectionRecovery can display the reason and actions. ```  </details>  </blockquote></details> <details> <summary>src/app/api/trips/[id]/hotels/route.ts-31-39 (1)</summary><blockquote>  `31-39`: _⚠️ Potential issue_ | _🟠 Major_  **Missing authentication/authorization.**  Unlike the sibling `approve` route (which requires `session`), this handler accepts any caller knowing a trip `id`, runs a geo query, and writes back to the Trip document. Add a session check and verify the authenticated user owns (or is assigned to) the trip before persisting.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/api/trips/`[id]/hotels/route.ts around lines 31 - 39, The POST handler lacks auth: retrieve the current session (using the same auth helper/getServerSession pattern used in the sibling approve route) at the start of POST, return 401 if no session, then after Trip.findById(id) verify the session user id owns or is assigned to the trip (e.g. compare session.user.id to trip.ownerId or trip.assignedTo) and return 403 if not authorized before performing any geo query or writing; keep using the existing symbols POST, context.params, connectToDatabase, Trip.findById and NextResponse.json to locate where to add the checks. ```  </details>  </blockquote></details> <details> <summary>src/app/hotels/page.tsx-48-60 (1)</summary><blockquote>  `48-60`: _⚠️ Potential issue_ | _🟠 Major_  **Seed button is publicly callable.**  This control triggers `POST /api/hotels/seed`, which (per the context snippet) `deleteMany({})`s the `preferred_vendors` collection and re-inserts. If this page is reachable in production without auth, any visitor can wipe and reseed preferred vendors at will. At minimum hide the button outside development, and gate the underlying route with auth + a dev-only flag.  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/hotels/page.tsx` around lines 48 - 60, The seed button and backend endpoint are unprotected: update the UI so seedDatabase (the client-side function calling POST "/api/hotels/seed") is only rendered/available in development (e.g., guard the button with a dev-only build flag like process.env.NEXT_PUBLIC_NODE_ENV === "development" or a NEXT_PUBLIC_ALLOW_SEED feature flag), and harden the API route handler for POST /api/hotels/seed by requiring authentication/authorization (check the current user/session) and a server-only dev flag (e.g., process.env.NODE_ENV === "development" or a server-only ALLOW_DB_SEED env var) before performing the dangerous deleteMany/insert operations on preferred_vendors; if checks fail return 403 and do not modify the DB. ```  </details>  </blockquote></details> <details> <summary>src/app/trip/[id]/approval/page.tsx-157-186 (1)</summary><blockquote>  `157-186`: _⚠️ Potential issue_ | _🟠 Major_  **Gate demo webhook controls behind an env flag.**  These buttons POST directly to `/api/webhooks/gmail-approval` with `approved: true/false` for the current `tripId`, bypassing any manager reply. If this page ships to production as-is, any user viewing the approval page can self-approve their own trip. Hide behind `process.env.NODE_ENV !== "production"` (or a dedicated `NEXT_PUBLIC_ENABLE_DEMO_CONTROLS`), and ensure the webhook route itself authenticates/authorizes the caller independently.   <details> <summary>🛡️ Suggested gate</summary>  ```diff -          {/* Manual approval for demo (simulate manager reply) */} -          <div className="border border-dashed border-gray-300 rounded-xl p-4 text-sm"> +          {process.env.NEXT_PUBLIC_ENABLE_DEMO_CONTROLS === "true" && ( +          <div className="border border-dashed border-gray-300 rounded-xl p-4 text-sm">              ... -          </div> +          </div> +          )} ``` </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/app/trip/`[id]/approval/page.tsx around lines 157 - 186, The demo approve/reject buttons on the Trip approval page call fetch("/api/webhooks/gmail-approval") directly (see the onClick handlers for the ✓ Approve and ✗ Reject buttons) and must be hidden behind an environment flag to prevent users from self-approving; wrap the UI block rendering these buttons in a check for a build-time/public flag like NEXT_PUBLIC_ENABLE_DEMO_CONTROLS (or NODE_ENV !== "production") so the controls are not rendered in production, and separately update the /api/webhooks/gmail-approval route to perform proper authentication/authorization of the caller (validate a token, session, or manager identity) rather than relying on the UI being hidden. ```  </details>  </blockquote></details> <details> <summary>src/components/receipts/ReceiptScanner.tsx-56-71 (1)</summary><blockquote>  `56-71`: _⚠️ Potential issue_ | _🟠 Major_  **Blob URL leak — `URL.createObjectURL` is never revoked.**  Every call to `handleFileChange` allocates a new object URL via `URL.createObjectURL(file)`, but the URL is never passed to `URL.revokeObjectURL`. The blob (and the underlying image bytes) is retained until the document is discarded. This leaks on every retake/reset and on component unmount.    <details> <summary>🧹 Proposed fix</summary>  ```diff -import { useState, useRef } from "react" +import { useState, useRef, useEffect } from "react" ```  ```diff    function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {      const file = e.target.files?.[0]      if (!file) return        setMimeType(file.type || "image/jpeg")      const url = URL.createObjectURL(file) -    setPreviewUrl(url) +    setPreviewUrl((prev) => { +      if (prev) URL.revokeObjectURL(prev) +      return url +    }) ```  ```diff    function handleReset() {      setScanState("idle") -    setPreviewUrl(null) +    if (previewUrl) URL.revokeObjectURL(previewUrl) +    setPreviewUrl(null)      setImageBase64(null)      setScanned(null)      setError(null)      if (fileInputRef.current) fileInputRef.current.value = ""    } ```  Plus an unmount cleanup effect: ```diff +  useEffect(() => { +    return () => { +      if (previewUrl) URL.revokeObjectURL(previewUrl) +    } +  }, [previewUrl]) ```  </details>  <details> <summary>🤖 Prompt for AI Agents</summary>  ``` Verify each finding against the current code and only fix it if needed.  In `@src/components/receipts/ReceiptScanner.tsx` around lines 56 - 71, handleFileChange currently calls URL.createObjectURL(file) and sets it via setPreviewUrl but never revokes it, leaking blob memory; update handleFileChange to revoke any existing object URL (the current preview URL from state) before creating a new one, then create the new URL with URL.createObjectURL(file) and setPreviewUrl(url). Also add a useEffect cleanup that revokes the preview URL on component unmount (and whenever preview URL changes) to ensure URL.revokeObjectURL(previewUrl) is called; reference the handleFileChange function, setPreviewUrl state, and the preview URL state when implementing these revocations. ```  </details>  </blockquote></details>  </blockquote></details>  ---  <details> <summary>ℹ️ Review info</summary>  <details> <summary>⚙️ Run configuration</summary>  **Configuration used**: defaults  **Review profile**: CHILL  **Plan**: Pro  **Run ID**: `9916622d-c122-4d2f-80f8-9c3ec7fcba38`  </details>  <details> <summary>📥 Commits</summary>  Reviewing files that changed from the base of the PR and between 96fa1460ebf392722591eb3e60537cbeb6995f07 and 3f91d2c44e52b87997c0452480f294997f5688ce.  </details>  <details> <summary>⛔ Files ignored due to path filters (3)</summary>  * `package-lock.json` is excluded by `!**/package-lock.json` * `src/app/favicon.ico` is excluded by `!**/*.ico` * `testing_receipt.jpg` is excluded by `!**/*.jpg`  </details>  <details> <summary>📒 Files selected for processing (51)</summary>  * `package.json` * `scripts/test-gemini-direct.mjs` * `scripts/test-receipt-scan.mjs` * `src/app/(auth)/login/page.tsx` * `src/app/api/auth/[...nextauth]/route.ts` * `src/app/api/auth/gmail-send/route.ts` * `src/app/api/auth/gmail-test/route.ts` * `src/app/api/hotels/search/route.ts` * `src/app/api/hotels/seed/route.ts` * `src/app/api/receipt/list/route.ts` * `src/app/api/receipt/save/route.ts` * `src/app/api/receipt/scan/route.ts` * `src/app/api/stt/route.ts` * `src/app/api/trips/[id]/approve/route.ts` * `src/app/api/trips/[id]/hotels/route.ts` * `src/app/api/trips/[id]/receipts/route.ts` * `src/app/api/webhooks/gmail-approval/route.ts` * `src/app/auth/page.tsx` * `src/app/demo/page.tsx` * `src/app/hotels/page.tsx` * `src/app/layout.tsx` * `src/app/page.tsx` * `src/app/receipt/page.tsx` * `src/app/receipt/saved/page.tsx` * `src/app/trip/[id]/approval/page.tsx` * `src/app/trip/[id]/planning/page.tsx` * `src/app/trip/[id]/post-trip/page.tsx` * `src/components/approval/ApprovalStatus.tsx` * `src/components/hotels/HotelCard.tsx` * `src/components/hotels/HotelMap.tsx` * `src/components/hotels/HotelMapInner.tsx` * `src/components/mascot/Mascot.tsx` * `src/components/providers/AuthProvider.tsx` * `src/components/receipts/ReceiptScanner.tsx` * `src/hooks/useMascot.ts` * `src/hooks/useTrip.ts` * `src/lib/gemini/client.ts` * `src/lib/gemini/multimodal.ts` * `src/lib/gemini/prompts.ts` * `src/lib/google/gmail.ts` * `src/lib/google/oauth.ts` * `src/lib/hotels/geoSearch.ts` * `src/lib/hotels/search.ts` * `src/lib/mongodb/client.ts` * `src/lib/mongodb/models/PreferredVendor.ts` * `src/lib/mongodb/models/Trip.ts` * `src/lib/utils/currency.ts` * `src/lib/utils/pii.ts` * `src/types/index.ts` * `src/types/next-auth.d.ts` * `src/types/receipt.ts`  </details>  <details> <summary>💤 Files with no reviewable changes (1)</summary>  * src/lib/mongodb/models/PreferredVendor.ts  </details>  </details>  <!-- This is an auto-generated comment by CodeRabbit for review status -->